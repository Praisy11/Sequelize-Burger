	{{#each burgers}}
    
	{{#if devoured}}
	
    <div>
<h4 class="devoured-burger-name">{{id}}. {{burger_name}} </h4>
<button class="delete-burger btn btn-primary" data-id="{{this.id}}"> Delete it</button>
		
    </div>
    {{/if}}
{{/each}}